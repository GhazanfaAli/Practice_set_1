import java.util.*;
// --> note write Ghazanfar ali but comments have written by Google bard.😉🤗☺
public class SumOf_evenNum {
    public static void main(String args[]){

// ==> using for loop

        // Print a message to the console asking the user to enter a number.
        System.out.print("enter a number: ");
        // Create a Scanner object to read input from the console.
        Scanner obj1 = new Scanner(System.in);
        // Read the number entered by the user and store it in the variable `n`
        int n = obj1.nextInt();
        // Initialize a variable `sum` to store the sum of the even numbers.
        int sum = 0;
        /*
        for(int i = 0; i<n; i++){ // -->  for loop iterates from 0 to n - 1.
            // Check if the current value of `i` is even.
            if(i % 2 == 0){
                // Add the current value of `i` to the variable `sum`.
            sum += i;
        }
        }  // Print the sum of the even numbers.
         System.out.println("SUM = "+sum);

//////////////////////////////////////////////////////////////////////////////////

 ==> using while loop

        int i = 0;
        while(i < n){
            if(i % 2 == 0){
                // Add the current value of `i` to the variable `sum`.
                sum += i;
            }
            i++;
        }
        // Print the sum of the even numbers.
        System.out.println("SUM = "+sum);

*/
    }
}
